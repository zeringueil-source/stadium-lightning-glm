# One NEXRAD radar site code per line — this is what fetch_nexrad.py
# actually pulls and decodes each run. Keep this list short (each site
# costs a 10-30MB download + decode every run) — it should normally just
# be whichever site(s) cover the venue(s) you're actively working with.
#
# The dashboard shows you the nearest site code/name once you pick a
# venue (e.g. "Nearest NEXRAD: KOHX — Nashville TN, 42 mi"). Add that
# code below to start getting real reflectivity data for it; the next
# scheduled run will pick it up.
#
# Lines starting with # are ignored.

KOHX
KHDC
